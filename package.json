{
  "name": "design-tokens",
  "version": "1.0.0",
  "description": "Design system token manager - parse, transform, validate, and document design tokens across platforms",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "design-tokens": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "ts-node src/index.ts",
    "test": "vitest run",
    "lint": "eslint src/",
    "prepublishOnly": "npm run build"
  },
  "keywords": ["design-tokens", "design-system", "css", "scss", "tailwind", "tokens"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "commander": "^11.1.0",
    "yaml": "^2.3.4",
    "chalk": "^4.1.2"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "@types/node": "^20.10.0",
    "ts-node": "^10.9.2",
    "vitest": "^2.0.0"
  }
}
